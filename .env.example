# Backend API URL (used by Vite proxy in development)
VITE_API_TARGET=http://localhost:5000

# Frontend API Base URL
# In development: use "/api" to leverage the Vite proxy
# In production: set to the full URL of your deployed backend (e.g., https://your-api.onrender.com/api)
VITE_API_BASE_URL=/api
